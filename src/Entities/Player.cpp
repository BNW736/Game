#include "Player.h"

void movement(int speed){
    if(side=1) { movements.x=movements.x + speed; }
    Else if(side=2) { movements.x=movements.x + speed; }
    Else if(side=3) { movements.x=movements.x + speed; }
    Else if(side=4) { movements.x=movements.x + speed; }
}
void draw(){
    drawrecternge()
}



